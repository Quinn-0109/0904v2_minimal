# Seed 1001: bounded progress and final heading alignment

Evidence: `fwd1001_s1001_20260912_121348`.

The mission stopped at 600.260 simulation seconds during descent. Floor
exploration took 174.531, 148.128 and 177.762 seconds (500.421 total).
The runner projected 626.9 seconds; that is an estimate, not a completed run.
All 24 scans completed. Official scoring reports TP=4, FP=1, FN=1;
`floor_2_room_2` has zero candidates in both scan batches (37 and 40 frames).
Changing a weak-track filter cannot recover those absent candidates.

`floor_0_room_1_g4` started at ROS time 81.464, recovered at 91.248,
and accepted a sensor-confirmed blocked target at 110.483. Its navigation
therefore took 29.019 seconds; the waypoint-complete duration additionally
includes scanning. The full high-frequency pose trace is unavailable, so
the precise amount attributable to watchdog resets is not proven.

The old watchdog overwrote both progress anchors when either metric improved.
Alternating positional drift and heading changes could consequently renew
the timeout without net improvement. Each anchor now advances independently;
the recovery count, obstacle evidence gates and timeout settings are retained.
Anchors still reset after an explicitly bounded recovery attempt.

Room G3/G4 final heading alignment uses proportional gain 1.8 instead of 1.1.
This applies only inside the existing position tolerance with the plane policy.
The existing yaw-rate cap and heading tolerance still apply. Translation,
transit yaw, scan angles and scan rates are unchanged. Set the private ROS
parameter `room_final_yaw_gain` to 1.1 before launching the sequencer to restore
the previous final alignment command. New `waypoint_alignment` events record
entry into and departure from the position-tolerance region.

Verification: 11 focused budget/detector tests and 3 existing handoff,
holonomic-path and bounded-recovery tests pass. No Gazebo run was performed
for this commit. Yaw response/overshoot, whole-mission duration and detection
quality require another run. This commit does not claim to solve the observed
false positive or missed source, and consumes no truth data in the controller.
